#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# Thin repo-local launcher: delegate to the canonical launcher script.
exec "$SCRIPT_DIR/../src/shell/core/kano-backlog" "$@"
