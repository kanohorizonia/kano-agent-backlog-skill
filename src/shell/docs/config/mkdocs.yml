# Retired MkDocs placeholder.
#
# Python API documentation is not part of the native executable milestone.
# The docs pipeline keeps this file only so legacy script arguments remain
# harmless while 04-deploy-mkdocs.sh writes a native API placeholder.

site_name: Kano Agent Backlog Skill Native API
site_description: Native executable contract for Kano Agent Backlog Skill
